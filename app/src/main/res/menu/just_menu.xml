<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/justMenu"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_btn_action_menu"
        android:title="">

        <menu>

            <item android:id="@+id/justMenuChangename"
                app:showAsAction="never"
                android:title="Change name"
                android:icon="@drawable/ic_btn_action_menu_change_name"/>
            <item android:id="@+id/justMenuExit"
                app:showAsAction="never"
                android:title="Exit"
                android:icon="@drawable/ic_btn_action_menu_exit"/>


        </menu>

    </item>

</menu>